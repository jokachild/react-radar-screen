
var initialState = {
    lat: 0,
    lng: 0,
    data: {
        name: ""
    }
};

export default function center(state = initialState, action = null) {

    switch(action.type) {
        default:
            return state;
    }

}